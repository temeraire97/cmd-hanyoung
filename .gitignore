.build/
*.app
.DS_Store
Testing.xcframework/
# Package.resolved는 의도적으로 추적 — 버전 재현성 보장
graphify-out/

# 아이콘 중간 산출물 — AppIcon.icns는 커밋 대상, 마스터 PNG만 제외
Resources/icon-master.png

# 릴리스 아티팩트 (GitHub 릴리스로 배포, repo 커밋 안 함)
dist/*.zip
dist/*.zip.sha256
